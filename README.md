# image-to-ascii

hello its image to ascii python script 

Usage:
python image_to_ascii.py path/to/image.jpg

Output:
<basename>ASCII.html (save next to the input file)

requires
    pip install Pillow


ENJOY!!! 

